<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Log Viewer</title>
  <style>
    body {
      font-family: 'Segoe UI', Arial, sans-serif;
      background: #f6f7fb;
      margin: 0;
    }

    .container {
      max-width: 700px;
      margin: 40px auto;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 8px 32px rgba(102, 166, 255, 0.12);
      padding: 32px;
    }

    h1 {
      color: #66a6ff;
      text-align: center;
    }

    .log-content {
      background: #eaf4ff;
      border-radius: 8px;
      padding: 18px;
      font-family: 'Fira Mono', 'Consolas', monospace;
      white-space: pre-wrap;
      color: #333;
      margin-top: 24px;
    }

    .error {
      color: #d32f2f;
      font-size: 1.1rem;
      margin-top: 18px;
      text-align: center;
    }

    form {
      text-align: center;
      margin-bottom: 24px;
    }

    input[type="text"] {
      padding: 8px;
      border-radius: 6px;
      border: 1px solid #66a6ff;
      margin-right: 8px;
    }

    button {
      background: #66a6ff;
      color: #fff;
      border: none;
      border-radius: 6px;
      padding: 8px 18px;
      cursor: pointer;
      transition: background 0.2s;
    }

    button:hover {
      background: #89f7fe;
      color: #333;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>Error Log Viewer</h1>
    <form action="/view" method="GET">
      <input type="text" name="file" placeholder="Enter log filename (e.g. error.txt)" required>
      <button type="submit">View Log</button>
    </form>
    <form action="/upload" method="POST" enctype="multipart/form-data" style="margin-bottom:24px; text-align:center;">
      <input type="file" name="logfile" accept=".txt" required>
      <button type="submit">Upload Log File</button>
    </form>
    <% if (error) { %>
      <div class="error">
        <%= error %>
      </div>
      <% } %>
        <% if (log) { %>
          <div class="log-content">
            <%= log %>
          </div>
          <% } %>
  </div>
</body>

</html>